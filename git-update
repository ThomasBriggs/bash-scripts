for i in $@
do
    if [ -d $i ]
    then
        cd $i
        if [ -e .git ]
        then
            git add .
            git commit -m "Update git folder"
            git push
        fi
        cd ..
    fi
done